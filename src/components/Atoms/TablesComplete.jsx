import React from 'react';

const TablesComplete = ({children, cls, table}) => (
  <figure className={cls}>
    <img src={table} alt="Tables-sector-9" />
  </figure>
);

export default TablesComplete;
